[project]
name = "duckify"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jupyter>=1.1.1",
    "tracing",
    "genai",
    "ui",
]

[project.scripts]
start = "ui.main:main"

[tool.uv.workspace]
members = [
    "genai",
    "tracing",
    "ui",
]

[tool.uv.sources]
genai = { workspace = true }
tracing = { workspace = true }
ui = { workspace = true }
