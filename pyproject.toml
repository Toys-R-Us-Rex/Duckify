[project]
name = "duckify"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jupyter>=1.1.1",
    "tracing",
    "ui"
]

[project.scripts]
start = "ui.main:main"

[tool.uv.workspace]
members = [
    "tracing",
    "ui",
]

[tool.uv.sources]
tracing = { workspace = true }
ui = { workspace = true }
